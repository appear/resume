import { Theme } from './types'

import common from './common'

const theme: Theme = {
  ...common,
  color: {
    background_color: '#151414',
    bio_tag: '#fd2e69',
    text: '#e0e2e4',
    link: '#d89a3e',
  },
}

export default theme
