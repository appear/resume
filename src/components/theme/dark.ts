import { Theme } from './types'

import common from './common'

const theme: Theme = {
  ...common,
  color: {
    backgroundColor: '#151414',
    text: '#e0e2e4',
    link: '#d89a3e',
  },
}

export default theme
