import { Theme } from './types'
import common from './common'

const theme: Theme = {
  ...common,
  color: {
    backgroundColor: '#fff',
    text: '#3a3a3a',
    link: '#368fff',
  },
}

export default theme
